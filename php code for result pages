<?php
session_start();
if (!isset($_SESSION["authenticated"]) || $_SESSION["authenticated"] !== true) {
    header("Location: index.html"); // Redirect back to login page if not authenticated
    exit();
}
?>
